/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'upload.wikimedia.org',
      'i.pravatar.cc',
      'images.unsplash.com',
    ]
  }
}

module.exports = nextConfig
